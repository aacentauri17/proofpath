{
  "name": "credkit",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:islands": "esbuild react-src/islands.jsx --bundle --minify --jsx=automatic --outfile=islands.bundle.js"
  },
  "dependencies": {
    "ogl": "^1.0.11",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
